урок5 - конец статика?

django-admin startproject ContractApp
python manage.py runserver
python manage.py startapp contract   --создание приложения
apps.py >> contract.apps.ContractConfig   --регистрация приложения
views.py >> from django.http import response
	views.py >>
		def home(request):
    		return HttpResponse('<h1>Hel wor</h1>')

 urls.py >> from contract.views import home
 urls >> urlpatterns >> path('', home)
 + contract/urls.py
 contract/urls.py >> from django.urls import path
 contract/urls.py >> from .views import *

 contract/urls.py >> 
 urlpatterns = [
    path('', home),
	]

 urls.py >> from django.urls import path, include

 urls.py >> urlpatterns >>     path('', include('contract.urls')),

 settings.py >>
 INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'contract.apps.ContractConfig',   <<<<<<
]

для работы с файлами:

settings.py >> 
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
MEDIA_URL = '/media/'

project >> urls.py >>
from django.conf import settings
from django.conf.urls.static import static

if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)

АДМИНКА:

python manage.py createsuperuser

регистрация моделей
admin.py >>
from .models import Users

class UsersAdmin(admin.ModelAdmin):
    list_display = ('name', 'role_id')
    # list_display_links = ('id', 'username', 'fio', 'created_at')
    # search_fields = ('username', 'fio')

admin.site.register(Users, UsersAdmin)

